# Taste
- User's active project is **Pathshala-Pro**, a Next.js school management ERP, located at `D:\my project\PathshalaProSchoolManagementSystemERP\Pathshala-Pro`. Code is organized under `src/app`, `src/components`, `src/lib`, and `src/viewmodels` (MVVM-style layering). Confidence: 0.85
- The app is fully internationalized across **4 locales** (`src/messages/en.json`, `ur.json`, `hi.json`, `bn.json`) — every user-facing string change must add/rename/delete keys in **all 4 files with exact parity** (identical keys and `{variable}` placeholders), enforced by `src/lib/i18n-parity-and-interpolation.test.ts` (re-confirmed in practice: new keys were added to all 4 locales and parity re-verified by script + suite). Confidence: 0.85
- **Excel (.xlsx) over CSV for all data exports** — user explicitly asked to migrate every CSV export to Excel and approved the "replace, don't keep both" plan. Exports go through the shared client-side engine (`src/hooks/use-excel-export.ts` → `exportData`, backed by `src/lib/excel-exporter.ts`) with translated column headers and the tenant school banner; new export features should be Excel-only, never CSV. Confidence: 0.9
- Development machine is **Windows** — POSIX commands like `mv` fail in the project shell, and the default shell is **cmd.exe without `grep`**; use cmd-compatible commands (`ren`, `findstr`) and backslash paths. PowerShell is available when richer scripting is needed. Confidence: 0.9
- The user's shell has **`NODE_ENV=production` set globally**, which makes vitest load production React builds (no `act`) and crash component tests with "React.act is not a function". Permanently fixed: `vitest.config.mts` now sets `process.env.NODE_ENV = "test"` before the config export (root cause empirically confirmed: ambient `NODE_ENV=production` leaked into vitest), so plain `npx vitest run` works on this machine. Confidence: 0.9
- **After a `schema.prisma` edit, the running Next.js dev server locks the Prisma engine DLL** — `prisma generate` fails with EPERM until the dev server stops. The reliable sequence on this machine is: stop dev server → `npm run prisma:push` → `npm run prisma:generate` → restart dev server. A stale generated client (schema says enum, client still expects `String`) surfaces as Prisma P2032 / engine type-mismatch errors at runtime. Confidence: 0.85
- The user runs a long-lived dev server (port 3001) they manage themselves — when offered kill-it-for-you vs. stop-it-myself, they chose to stop it on their own. Ask before terminating user-owned processes, and prefer letting them do it. Confidence: 0.6
- **Prefers centralized, app-wide settings over per-screen repetition** — user proposed moving the academic-year selector to the top header so users pick it once instead of on every screen, and as soon as it shipped asked whether ALL per-screen year selectors could now be removed. When introducing user-selectable context, default to global state (persisted per user, surviving reloads/navigations) with per-screen overrides only where a different value is legitimately needed. Apply removal carefully: strip redundant per-screen *view filters* that merely mirror the header (fees/structures, fees/bulk, promotion-rules filter, performance session picker), but KEEP form-level fields that are record data — exam create/edit, fee structure form, batch invoice modal, promotion rule form — plus the promotion calculator's URL-param override. Rule of thumb: header = what I'm looking at; form field = what I'm creating. Confidence: 0.8
- **Wants blunt, honest evaluation of ideas** — repeatedly appends "ANSWER ONLY" (and "be truthful") to design questions; expects a plain direct answer first, not immediate code changes. Give a direct verdict up front, then honest caveats/risks; no flattery, no padding. Confidence: 0.8
- Client data fetching uses **TanStack Query** — global filters/selectors (e.g., academic year) must be included in query keys or cached data goes stale across screens. Confidence: 0.6
- **Once the user signs off on an approach, implement it end-to-end without further check-ins** — their response to an agreed plan is "OKEY THAN IMPLEMENT THIS AND GIVE ME THE RESULT" / "OKEY FINE, DO AS YOU WANT": explore the codebase, build, wire every call site, add i18n, verify, and hand back the finished result in one pass. They also delegate the *decision itself* ("DO AS YOU WANT") once the trade-offs have been laid out — after a clear reasoned recommendation, proceed with it autonomously. No intermediate approvals, no partial deliverables. Confidence: 0.8
- **Verification gate before declaring work done**: run `npm run test:all` (strict `tsc --noEmit` + full vitest suite) and `npx eslint` on the touched files, and report the green numbers in the summary (baseline after the global academic-year feature: 50 suites / 383 tests passing, 0 lint errors). Confidence: 0.7
- **vitest v4 gotcha**: node builtins (`fs`, `path`) are no longer shimmed under the jsdom environment — lib test suites that use them need a `// @vitest-environment node` pragma at the top of the file. Confidence: 0.7
- The repo carries a root **`AGENTS.md`** of coding conventions that agents are expected to honor. It prohibits UI anti-patterns: native `<button>`s, raw `<select>`/`<input>` and `type="date"` fields, and hand-rolled `fixed inset-0` modal overlays — new work must go through the shared UI primitives (`Button`, the `Input`/date primitives, `AppModal`/`TopSheet`, `StatusBadge`) instead. When auditing, scan against these AGENTS.md rules and treat existing violations as drift to fix. Confidence: 0.6
- **Glob-based tools mis-handle Next.js dynamic-route paths**: `[id]` in a path is parsed as a glob character class, so `read_file` fails on e.g. `src/app/(dashboard)/exams/[id]/page.tsx`. Read via PowerShell `Get-Content -LiteralPath '...\[id]\page.tsx'`, and use `edit_file` (which takes paths literally) for changes. Confidence: 0.6
- **Wants polish on initial loading states — no flash of zero/empty content before real data.** The dashboard must render skeletons from the first paint instead of showing `0` / `₨ 0` / `—` (or a table's "no records" empty state), then flipping to a skeleton, then to data. TanStack Query gotcha behind this: a **disabled** query reports `isLoading === false`, so deriving `isLoading` from the query flag alone (while queries are gated on auth/academic-year readiness) paints empty values. Derive loading from explicit readiness flags (auth ready, academic year ready) and gate each query only on the readiness it actually depends on, so dependent queries never fetch twice (e.g. once with an undefined `academicYearId`, then again once resolved). Confidence: 0.6
